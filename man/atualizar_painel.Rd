% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deploy_panel.R
\name{atualizar_painel}
\alias{atualizar_painel}
\title{Atualiza um esqueleto de painel ja gerado, preservando edicoes locais}
\usage{
atualizar_painel(diretorio = "painel", forcar = FALSE)
}
\arguments{
\item{diretorio}{caminho do diretorio da app (default "painel")}

\item{forcar}{substitui tambem arquivos modificados localmente
(default FALSE)}
}
\value{
caminho absoluto do diretorio da app (invisivel)
}
\description{
Companheira de \code{\link[=deploy_panel]{deploy_panel()}} com \code{esqueleto = TRUE}: quando uma
versao nova do AEDi traz melhorias no painel (bug fix de modulo, novo
asset), \code{atualizar_painel()} absorve essas mudancas no esqueleto ja
materializado no projeto \strong{sem destruir o que o projeto adaptou}.
}
\details{
O mecanismo usa o \code{esqueleto_manifest.json} gerado junto com a app:
para cada arquivo previsto nesta versao,
\itemize{
\item \strong{intacto} (hash local igual ao hash do manifest) — substituido
pela versao nova do AEDi;
\item \strong{modificado localmente} — preservado como esta e reportado no
fim (o hash upstream esperado continua no manifest, entao a
divergencia segue detectavel na proxima atualizacao); mescle as
mudancas manualmente ou repontue com \code{forcar = TRUE};
\item \strong{novo nesta versao} — adicionado; arquivo previsto antes e
retirado do esqueleto — apenas reportado, nunca apagado.
}

\code{forcar = TRUE} substitui inclusive os arquivos com edicoes locais
(destructivo — descarta as adaptacoes do projeto).
}
