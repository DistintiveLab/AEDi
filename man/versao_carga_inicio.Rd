% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controle_execucao.R
\name{versao_carga_inicio}
\alias{versao_carga_inicio}
\title{Registra o inicio de um ciclo de carga (modelo A: snapshot por ciclo)}
\usage{
versao_carga_inicio(
  dir_dump = "~/backups_aedidb",
  espelho = espelho_padrao(),
  codigo_versao = git_commit_aedi(),
  projeto = "AEDi"
)
}
\arguments{
\item{dir_dump}{diretorio dos dumps (default ~/backups_aedidb)}

\item{espelho}{diretorio espelho opcional (default: extrainters se montado)}

\item{codigo_versao}{identificador do codigo (default: commit git do AEDi)}

\item{projeto}{nome do projeto (raiz do lote) dono da versao de carga}
}
\description{
Executa pg_dump do aedidb para \if{html}{\out{<dir>}}/aedidb_v\if{html}{\out{<N>}}_\if{html}{\out{<AAAAMMDD>}}.dump e cria a
linha da versao. Ciclos SEM alteracao de dado nao geram dump (a versao
referencia o dump anterior). Dumps espelhados em \code{espelho} (ex.: montagem
sshfs extrainters) quando informado.
}
\details{
Retencao (aplicada ao final do ciclo, ver \code{versao_carga_fim()}):
semanais completos + ultimos 3 dias uteis de dumps.
}
