% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/incorporar_municipio_ibge.R
\name{incorporar_municipio_ibge}
\alias{incorporar_municipio_ibge}
\title{Incorpora um municipio criado depois da carga original do DW}
\usage{
incorporar_municipio_ibge(
  geoloc_id,
  municipio_referencia = NULL,
  grupos = NULL,
  tolerancia = 0.01,
  con = NULL,
  regenerar_recortes = TRUE
)
}
\arguments{
\item{geoloc_id}{codigo IBGE do municipio, 6 ou 7 digitos}

\item{municipio_referencia}{geoloc_id (7d) ou local_id de um municipio
vizinho de onde copiar as participacoes (default: primeiro municipio
da mesma regiao imediata do novo municipio)}

\item{grupos}{ids de datagroup adicionais a vincular (ex.: tipologia)}

\item{tolerancia}{dTolerance (graus) da simplificacao da malha;
default 0.01, calibrado com a resolucao das geometrias existentes}

\item{con}{conexao DBI com o DW; default abre o aedidb local}

\item{regenerar_recortes}{regenerar a matview recortes_geograficos?}
}
\value{
\code{local_id} do municipio incorporado, invisivel
}
\description{
Baixa metadados e geometria da API do IBGE, insere \code{geoloc} (malha
simplificada) e \code{local} (local_id = max + 1, sempre apos o bloco PNAD,
7088, 7089, ...), vincula os recortes geograficos (regiao imediata e
intermediaria resolvidas pelo nome do IBGE; participacoes em Amazonia
Legal, faixa de fronteira, semiarido e SUDENE copiadas de um municipio
de referencia da mesma regiao imediata) e regenera a matview
\code{recortes_geograficos}. A tipologia PNDR nao e copiada nem resolvida —
o IBGE ainda nao publica classificacao para municipios novos; passe o
id do grupo via \code{grupos} quando existir. DATASUS tambem costuma demorar
a publicar populacao de municipios novos: indicadores que dependem de
popmun ficam com o valor do municipio pendente ate la (scripts devem
tolerar NA; ver massa_salarial_municipal.R).
}
