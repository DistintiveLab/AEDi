% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verifica_fonte.R
\name{verificar_novidade_fonte}
\alias{verificar_novidade_fonte}
\title{C5: verifica, SEM executar o script, se a fonte tem dados mais recentes
do que o que ja consta (DW ou cache). Retorno:
list(pular = logical, motivo = character, assinatura = character(1)).
Em duvida (fonte desconhecida, primeira carga, probe fora do ar),
pular = FALSE, ou seja, executa a coleta.}
\usage{
verificar_novidade_fonte(nome_script, raiz = .aedi_raiz(), con = NULL)
}
\arguments{
\item{nome_script}{nome do script de coleta (sem .R)}

\item{raiz}{raiz do projeto AEDi}

\item{con}{conexao aberta opcional}
}
\description{
C5: verifica, SEM executar o script, se a fonte tem dados mais recentes
do que o que ja consta (DW ou cache). Retorno:
list(pular = logical, motivo = character, assinatura = character(1)).
Em duvida (fonte desconhecida, primeira carga, probe fora do ar),
pular = FALSE, ou seja, executa a coleta.
}
\keyword{internal}
