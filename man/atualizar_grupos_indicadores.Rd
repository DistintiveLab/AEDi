% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grupos_catalogo.R
\name{atualizar_grupos_indicadores}
\alias{atualizar_grupos_indicadores}
\title{Declara e reconcilia os grupos tematicos do catalogo no DW}
\usage{
atualizar_grupos_indicadores(con = NULL, simular = FALSE, verbose = TRUE)
}
\arguments{
\item{con}{conexao aberta com o DW; por padrao abre uma com as variaveis
de ambiente \code{user}, \code{password}, \code{host} e \code{dbname} (mesmos defaults de
\code{\link[=db_datawrite]{db_datawrite()}})}

\item{simular}{se \code{TRUE} nao escreve nada: devolve apenas o plano}

\item{verbose}{imprime o resumo do que foi feito}
}
\value{
invisivel, o plano aplicado (listas \code{grupos}, \code{pais}, \code{vinculos} e
\code{divergentes})
}
\description{
Idempotente e nao destrutivo: cria os \code{datagroup}/\code{group_parent} que faltam
(Eixos, Objetivos, Estratos PNAD e os grupos Eixo 1..7 / Objetivo 1..4),
completa os vinculos de \code{mdata_group} de todos os indicadores que seguem a
convencao de \code{orig_name} e relata os vinculos que fogem dela — sem apagar
nenhuma linha. Rode depois de ETLs que adicionem indicadores; num DW recem
criado (sem indicadores) apenas declara os grupos.
}
