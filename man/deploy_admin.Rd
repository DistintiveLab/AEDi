% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deploy_admin.R
\name{deploy_admin}
\alias{deploy_admin}
\title{Materializa o painel admin do projeto (launcher)}
\usage{
deploy_admin(diretorio = "admin", raiz = NULL, sobrescrever = FALSE)
}
\arguments{
\item{diretorio}{caminho do diretorio da app, relativo ao projeto
(default "admin")}

\item{raiz}{raiz do projeto orquestrado (default: diretorio corrente);
usada apenas para nomear o projeto no README}

\item{sobrescrever}{substitui um app.R ja existente (default FALSE)}
}
\value{
caminho absoluto do diretorio da app (invisivel)
}
\description{
Companheira de \code{\link[=admin_app]{admin_app()}}: grava na raiz do projeto chamador uma
launcher Shiny (\verb{<diretorio>/app.R}) que monta o painel admin somente
leitura a partir do pacote AEDi instalado. Launcher fino por design:
correcoes e novas abas chegam com o reinstall do AEDi, sem copias
derivadas para divergirem.
}
\details{
A app le o banco aedidb com as mesmas credenciais do orquestrador
(variaveis de ambiente \code{user}, \code{password}, \code{host}, \code{dbname}) e so faz
sentido em localhost/LAN. NAO publicar em host acessivel externamente
sem autenticacao na frente (shinymanager/shinyproxy/VPN): a app expoe
internals do controle e depende de acesso direto ao banco.
}
