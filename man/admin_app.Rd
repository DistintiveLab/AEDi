% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admin_app.R
\name{admin_app}
\alias{admin_app}
\title{Painel admin (somente leitura) do lote de um projeto}
\usage{
admin_app(raiz = NULL, projeto = NULL, titulo = NULL)
}
\arguments{
\item{raiz}{raiz do projeto orquestrado (default: diretorio corrente);
define o lote (\verb{coleta/}) e o projeto no controle (\code{basename})}

\item{projeto}{nome do projeto no controle_execucao (default:
\code{.nome_projeto(raiz)}); informar explicitamente para monitorar um
lote hospedado em outro caminho}

\item{titulo}{titulo da janela/aba}
}
\value{
objeto \code{shiny_app} (usar via \code{\link[=deploy_admin]{deploy_admin()}} ou
\code{shiny::runApp()})
}
\description{
App Shiny autonoma de monitoramento do orquestrador para o projeto
chamador: status de cada script de coleta (com o motivo pelo qual foi
pulado, inclusive dependencias), grafo de dependencias declarado, frescor
das series no banco e historico das execucoes. Somente leitura por
design: disparar execucoes continua sendo via cron ou app AEDi (o
gatilho manual exige lock single-flight, ver
\code{roadmap_dependencias_orquestrador.md}).
}
\details{
Companheira de \code{\link[=deploy_admin]{deploy_admin()}}, que materializa uma launcher desta app
na raiz do projeto. Conexao: banco aedidb via variaveis de ambiente
(\code{user}, \code{password}, \code{host}, \code{dbname}), as mesmas do orquestrador.
}
